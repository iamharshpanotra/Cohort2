function findSum(a,b) {
    //Goal of a function: It does things with the input and returns the output
    const sumValue = a + b;
    return sumValue;
}

const sum = findSum(5,10);
console.log(sum);


// Nikhil["name"] ======= Nihil.name